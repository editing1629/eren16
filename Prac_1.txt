# Step 1: Import Required Libraries
import pandas as pd
import numpy as np

# Step 2 & 3: Load Dataset
df = pd.read_csv("titanic.csv")

# Rename column
df.rename(columns={'Sex': 'Gender'}, inplace=True)

# Display first 5 rows
print(df.head())

# Step 4: Data Preprocessing

# Check missing values
print(df.isnull().sum())

# Handle missing values
df['Age'].fillna(df['Age'].mean(), inplace=True)

# Basic statistics
print(df.describe())

# Dataset info
print(df.info())

# Check dimensions
print(df.shape)

# Column names
print(df.columns)

# Step 5: Data Formatting & Normalization

# Check data types
print(df.dtypes)

# Convert data types
df['Gender'] = df['Gender'].astype('category')
df['Pclass'] = df['Pclass'].astype('category')

# Normalize Age column
df['Age'] = (df['Age'] - df['Age'].min()) / (df['Age'].max() - df['Age'].min())

# Step 6: Convert Categorical to Numerical

# Label Encoding
df['Gender'] = df['Gender'].cat.codes

# OR One-Hot Encoding (optional)
# df = pd.get_dummies(df, columns=['Gender'])

# Final output
print(df.head())                 CODE is effieceint for the practical exam and this problem statement